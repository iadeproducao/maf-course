# 02 - Instalação

## Requisitos
- Python 3.12+
- uv

## Passos
1. Instalar o `uv`:

```bash
python -m pip install uv
```

2. Sincronizar as dependências do projeto:

```bash
uv sync
```

3. Configurar variáveis em `.env`.
